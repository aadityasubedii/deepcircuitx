{
    "module": "Module-level comment: The C_wave module generates a clock-driven waveform output. It increments a 12-bit `step_counter` on each `dac_lrclk` positive edge, using this counter to index a waveform from the `C_LUT`. The 16-bit `wave` output conveys the waveform data from `C_LUT` to external systems."
}