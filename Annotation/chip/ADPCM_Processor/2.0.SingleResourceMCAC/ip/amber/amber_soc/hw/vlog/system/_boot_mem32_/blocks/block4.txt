Line number: 
[143, 149]
Comment: 
This block defines a conditional compilation section for a generic SRAM module with byte enable, specific to non-Xilinx FPGAs. Within this clause, the SRAM module is instantiated generically with set parameter widths. Module parameters include DATA_WIDTH which refers to the width of the data bus and it is set as WB_DWIDTH, and ADDRESS_WIDTH which refers to the width of the address bus, set as MADDR_WIDTH.