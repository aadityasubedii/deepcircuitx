Line number: 
[131, 185]
Comment: 
This block of code serves two primary functions, which are conditional upon the status of the FULL_BARREL parameter. The block either performs a full barrel left shift operation or a quick left shift, depending on whether FULL_BARREL equals 1. When performing a full barrel shift, the block uses a multi-way conditional to check for a shift amount equal to zero, an immediate-shift-zero condition, and different shift amounts ranging from 1 to 32 bits; it then acts accordingly. Conversely, if FULL_BARREL equals 0, the block performs a quicker 4-way shift operation with shift amounts of 0 to 3 bits. Its implementation uses concatenation and slicing of bit vectors to achieve the appropriate shift operations.