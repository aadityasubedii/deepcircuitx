Line number: 
[2581, 2581]
Comment: 
This block triggers a data quality service (DQS) positive edge timing check. It does so by evaluating the 13th bit of the `dqs_in` input signal on every rising edge. The function `dqs_pos_timing_check(13)` is called to perform the timing check.