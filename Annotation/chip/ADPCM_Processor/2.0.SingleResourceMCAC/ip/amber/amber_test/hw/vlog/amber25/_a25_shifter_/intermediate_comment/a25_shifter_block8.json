{
    "block_comment": "This block performs a logical shift left (LSL) operation. Depending on the value of `i_shift_imm_zero` or `i_shift_amount`, it shifts the input `i_in` by 0, 1, 2, or 3 places to the left and assigns `i_carry_in` as the new LSB, while the odd bits get shifted into the MSB."
}