Line number: 
[468, 468]
Comment: 
This block assigns a non-inverting tri-state differential data strobe signal (`tdqs_n`). The function is achieved by assigning an n-bit bus of high impedance state (`1'bz`) to `tdqs_n`, where n is equivalent to `DQS_BITS`. The high impedance state is used for bus isolation.