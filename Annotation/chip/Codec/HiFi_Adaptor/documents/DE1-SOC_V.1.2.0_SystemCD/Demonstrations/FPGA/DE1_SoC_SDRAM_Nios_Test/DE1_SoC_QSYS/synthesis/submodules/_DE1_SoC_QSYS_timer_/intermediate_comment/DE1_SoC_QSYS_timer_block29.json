{
    "block_comment": "This block implements a resettable and reloadable counter. On a rising clock edge or a falling reset, the counter either initializes to a specific value (0x22E97), reloads from `counter_load_value`, or decrements. Reload and decrement behavior depend on `counter_is_running`, `force_reload`, and `counter_is_zero` signals."
}