Line number: 
[183, 189]
Comment: 
This block implements a snapshot functionality for a counter. On a positive clock edge or a negative reset, it resets the `counter_snapshot` to 0 if `reset_n` is low, or updates it with `internal_counter` value on `snap_strobe` signal, preserving the counter value at specific instances.