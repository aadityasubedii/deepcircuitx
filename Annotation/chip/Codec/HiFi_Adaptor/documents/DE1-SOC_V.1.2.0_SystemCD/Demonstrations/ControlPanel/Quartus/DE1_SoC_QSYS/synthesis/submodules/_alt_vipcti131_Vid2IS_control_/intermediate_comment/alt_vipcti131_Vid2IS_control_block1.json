{
    "block_comment": "This block of code sets `is_std` to a value that extends `vid_std` to a 16-bit width. It achieves this by concatenating zeroes to the left of `vid_std` until the total width is 16 bits."
}