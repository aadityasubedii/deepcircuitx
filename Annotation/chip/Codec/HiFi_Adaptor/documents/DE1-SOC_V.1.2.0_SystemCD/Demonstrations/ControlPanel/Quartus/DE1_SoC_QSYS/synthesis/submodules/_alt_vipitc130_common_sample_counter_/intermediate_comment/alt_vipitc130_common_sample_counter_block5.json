{
    "block_comment": "This block determines if all color planes have valid count data. It checks if the `count_valids` equals one less than the `NUMBER_OF_COLOUR_PLANES`, setting `new_sample` true when the condition is met, indicating the completion of the sampling process."
}