{
    "block_comment": "This block defines a bidirectional port `bidir_port` for data transmission. The port direction is controlled by the `data_dir` signal: if `data_dir` is high (`1`), `bidir_port` outputs `data_out`; if low (`0`), it enters a high-impedance state (`1'bZ`), effectively disconnecting from the bus."
}