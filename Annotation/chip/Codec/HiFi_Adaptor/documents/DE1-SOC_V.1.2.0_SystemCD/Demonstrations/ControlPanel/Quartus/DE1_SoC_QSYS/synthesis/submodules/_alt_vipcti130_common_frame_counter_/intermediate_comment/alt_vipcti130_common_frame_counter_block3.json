{
    "block_comment": "This block calculates the total vertical resolution after subtracting one. It does this by subtracting a constant value (1, represented in 13-bit format) from the variable `v_total`, assigning the result to `v_total_int`."
}