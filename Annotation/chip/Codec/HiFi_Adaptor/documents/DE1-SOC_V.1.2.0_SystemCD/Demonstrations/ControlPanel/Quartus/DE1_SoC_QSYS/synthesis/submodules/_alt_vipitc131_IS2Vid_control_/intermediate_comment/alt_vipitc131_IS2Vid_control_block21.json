{
    "block_comment": "This block conditionally assigns a subset of the `is_mode_match` array to the `is_mode_match_output`. If the integer `NO_OF_MODES_INT` is 16 or greater, the first 16 elements (bits 0 to 15) of the `is_mode_match` array are assigned to `is_mode_match_output`."
}