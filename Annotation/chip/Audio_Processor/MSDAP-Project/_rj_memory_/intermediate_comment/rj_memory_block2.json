{
    "block_comment": "The block represents a memory write operation in a synchronous system. Triggered by the negative edge of the system clock (Sclk), the block assigns the input data (data_in) into the memory location specified by the write address (rj_wr_addr) whenever the write enable signal (wr_en) is active."
}