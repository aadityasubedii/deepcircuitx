{
    "block_comment": "This block calculates the center point between two edge coordinates. It does this by constantly monitoring the `smallest_right_edge` and `largest_left_edge` values. Whenever either of these values changes, the block recalculates the center point, adding the new `smallest_right_edge` and `largest_left_edge` values together using verilog's \"+\" operation."
}