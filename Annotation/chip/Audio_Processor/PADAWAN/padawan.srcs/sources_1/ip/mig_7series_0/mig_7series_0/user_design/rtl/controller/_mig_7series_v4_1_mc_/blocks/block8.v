      mig_7series_v4_1_ecc_gen #
        (
          
          .CODE_WIDTH               (CODE_WIDTH),
          .DATA_WIDTH               (DATA_WIDTH),
          .ECC_WIDTH                (ECC_WIDTH)
        )
        ecc_gen0
          (
            
            .h_rows                 (h_rows)
          );
