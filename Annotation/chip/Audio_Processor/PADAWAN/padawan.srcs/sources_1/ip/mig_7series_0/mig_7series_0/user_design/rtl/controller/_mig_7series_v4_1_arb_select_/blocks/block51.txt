Line number: 
[361, 375]
Comment: 
This block of code acts as a gate-level sequencer for certain signals in a memory controller, specifically for the precharge (pre_cmd_ns) operation. On the system reset (rst), the precharge command (pre_cmd_ns) is initialized to all zeros. When not under reset, pre_cmd_ns holds its previous value. Meanwhile, for each bank machine (represented by the variable 'i'), if a precharge grant (grant_pre_r[i]) is received then pre_cmd_ns is updated with rank, bank, row address, RAS (Row-Address-Strobe), CAS (Column-Address-Strobe), and write command information. This is achieved using looping, conditional checking, and selective concatenation of signal subsets.
