{
    "block_comment": "This block is a synchronous register update logic. The register `R_ctrl_implicit_dst_retaddr`, which is either reset to 0 upon a low signal on `reset_n` or updated at the next rising edge of `clk`, if `R_en` has a high value."
}