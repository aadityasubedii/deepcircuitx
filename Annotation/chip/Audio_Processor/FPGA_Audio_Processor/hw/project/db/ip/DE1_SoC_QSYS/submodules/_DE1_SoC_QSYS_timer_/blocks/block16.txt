Line number: 
[130, 139]
Comment: 
The code block acts as flip-flop logic that asserts a `timeout_occurred` flag based on various conditions. Upon a negative edge of the `reset_n` signal, `timeout_occurred` is de-asserted, restoring default state. On positive `clk` edge, if `clk_en` is high and `status_wr_strobe` is asserted, `timeout_occurred` flag is reset again. However, if a `timeout_event` happens instead, `timeout_occurred` signal is set high. Thus, it effectively monitors various control signals to maintain or alter `timeout_occurred` status.