Line number: 
[167, 176]
Comment: 
The block of code is responsible for managing system reset conditions and maintaining the status of the `middle_of_low_level` flag based on the `clk_counter` state. If the `reset` signal is high, it will set the `middle_of_low_level` flag to 0. Otherwise, it will compute the flag status by calculating the bitwise AND of the inverse of the designated bits of `clk_counter`. The calculation specifically involves the highest two bits and the logical AND of all the bits excluding the highest and lowest bit in `clk_counter`.